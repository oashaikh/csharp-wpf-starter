<Window x:Class="CsharpWpfStarter.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:CsharpWpfStarter.App.ViewModels"
        Title="C# WPF Starter"
        Height="360" Width="540"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Status}"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Measurements}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm:ss'}" Margin="0,0,12,0"/>
                        <TextBlock Text="{Binding Value, StringFormat='F2'}" Width="60" TextAlignment="Right"/>
                        <TextBlock Text="{Binding Unit}" Margin="4,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Start"
                    Command="{Binding StartCommand}"
                    Padding="12,4"
                    Margin="0,0,8,0"/>
            <Button Content="Stop"
                    Command="{Binding StopCommand}"
                    Padding="12,4"/>
        </StackPanel>
    </Grid>
</Window>
